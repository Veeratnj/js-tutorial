// 103_project_chat_app.js
// Chat app starter notes: use WebSockets for realtime messaging

// Client (browser) example:
// const ws = new WebSocket('wss://your-server');
// ws.addEventListener('message', e => console.log('msg', e.data));

console.log('Chat App starter: wire a WebSocket server and exchange JSON messages.');