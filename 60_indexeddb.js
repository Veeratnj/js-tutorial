// 60_indexeddb.js
// Simple IndexedDB open example (browser)

// const req = indexedDB.open('my-db', 1);
// req.onupgradeneeded = () => { const db = req.result; db.createObjectStore('notes', { keyPath: 'id' }); };
// req.onsuccess = () => console.log('DB opened');

console.log('IndexedDB provides structured storage in the browser.');