// 49_browser_apis.js
// Examples of common browser APIs (geolocation, history)

// Geolocation example (browser):
// navigator.geolocation.getCurrentPosition(pos => console.log(pos.coords));

// History API example (browser):
// history.pushState({page:1}, 'title', '?page=1');

console.log('Browser APIs expose features like geolocation, history, notifications.');