// 100_authentication_jwt.js
// JWT authentication example (using jsonwebtoken library)

// const jwt = require('jsonwebtoken');
// const token = jwt.sign({ userId: 1 }, 'secret', { expiresIn: '1h' });
// const payload = jwt.verify(token, 'secret');
// console.log(payload);

console.log('JWT example commented out — install jsonwebtoken to use.');