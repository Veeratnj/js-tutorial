// 58_server_sent_events.js
// Server-Sent Events (EventSource) example (browser)

// const es = new EventSource('/events');
// es.onmessage = e => console.log('SSE:', e.data);

console.log('SSE: use EventSource to receive push messages from the server.');